<%= form_for(@dumblaws) do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %><br />
	<%= f.label	:description %>
	<%= f.text_field :description %><br />
	<%= f.label :link %>
	<%= f. text_field :link %><br />
	<%= submit_tag("submit") %>

<% end %>